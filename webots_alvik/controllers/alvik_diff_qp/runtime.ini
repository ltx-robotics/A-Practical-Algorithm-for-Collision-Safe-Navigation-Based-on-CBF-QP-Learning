; Webots launches controllers with its own bundled Python by default, which has
; neither torch nor the project dependencies. Point it at the same interpreter
; the training used so the controller runs exactly the safe_alvik code that
; produced the checkpoint.
[python]
COMMAND = D:\Anaconda\envs\pytorch\python.exe
